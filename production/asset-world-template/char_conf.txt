// GENERATED by render-assets.sh — do not edit this file.
// Edit asset-world-template/char_conf.txt or the world env file, then re-render.
//
// --- World ${WORLD} char server ---

// Register with the single login server (internal, over the docker network).
login_ip: login

// Interserver account for THIS world. Its account_id (ACCOUNT_ID in the env
// file) is the world's slot in the login server (unique, 0-4). A matching
// sex='S' row must exist in the accounts DB `login` table (multiworld-init-db.sh).
userid: ${SERVER_USERID}
passwd: ${SERVER_PASSWORD}

// Name shown on the client's world-select screen.
server_name: ${SERVER_NAME}

// Port this char server listens on AND advertises (unique per world).
char_port: ${CHAR_PORT}

// Address advertised TO THE CLIENT -> your public DNS / HAProxy address.
char_ip: ${PUBLIC_IP}
