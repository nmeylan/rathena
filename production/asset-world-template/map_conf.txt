// GENERATED by render-assets.sh — do not edit this file.
// Edit asset-world-template/map_conf.txt or the world env file, then re-render.
//
// --- World ${WORLD} map server ---

// Reach this world's char server (internal). Use the unique container name,
// since the plain `char` alias is shared across worlds on the same network.
char_ip: rathena-world-${WORLD}-char
char_port: ${CHAR_PORT}

// Interserver credentials — must match this world's char server.
userid: ${SERVER_USERID}
passwd: ${SERVER_PASSWORD}

// Port this map server listens on AND advertises (unique per world).
map_port: ${MAP_PORT}

// Address advertised TO THE CLIENT -> your public DNS / HAProxy address.
map_ip: ${PUBLIC_IP}
