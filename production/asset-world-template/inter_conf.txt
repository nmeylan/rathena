// GENERATED by render-assets.sh — do not edit this file.
// Edit asset-world-template/inter_conf.txt or the world env file, then re-render.
//
// World ${WORLD} char/map/log DB connection -> the `${WORLD_DB}` database.
// Accounts are NOT here; they live in the login stack's `accounts` DB and are
// reached over the interserver link.

char_server_ip: db
char_server_id: ${DB_USER}
char_server_pw: ${DB_PASSWORD}
char_server_db: ${WORLD_DB}

map_server_ip: db
map_server_id: ${DB_USER}
map_server_pw: ${DB_PASSWORD}
map_server_db: ${WORLD_DB}

log_db_ip: db
log_db_id: ${DB_USER}
log_db_pw: ${DB_PASSWORD}
log_db_db: ${WORLD_DB}
