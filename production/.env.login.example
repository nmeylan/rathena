# Copy to .env (used by docker-compose.login.yml). Keep out of git.
#   cp .env.login.example .env
DB_ROOT_PASSWORD=change_me_long_random_root_pw
DB_NAME=accounts
DB_USER=ragnarok
DB_PASSWORD=change_me_long_random_app_pw

# DB_PASSWORD must match every *_pw value in asset-login/*.txt and
# asset-world-*/inter_conf.txt (rAthena and MariaDB are separate systems).
